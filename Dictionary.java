import java.io.IOException;
import java.nio.charset.StandardCharsets;
import java.nio.file.Path;
import java.nio.file.Paths;
import java.util.ArrayList;
import java.util.List;
import java.util.Scanner;

import static java.nio.charset.StandardCharsets.UTF_8;

public class Dictionary {
    static List<Word> listWord = new ArrayList<Word>();
//    public void SetDictionnary(String c,String b){
//        Word a=new Word();
//        a.setWordTarget(c);
//        a.setWordExplain(b);
//        listWord.add(a);
//    }

    /*public String xemtu(int i){
        return list.get(i);
    }
    public void getWord(String tuCanTim) // tim tu
    {
        int n1=tuCanTim.length();
        int n=list.size();
        for(int i=0;i<n;i++) {
            String a = list.get(i).substring(0, n1);
            if (a.equals(tuCanTim)){
                System.out.println(list.get(i));
                break;
            }
        }
    }   
    public void setWord(String tuCanSua,String yNghia) // sua tu bi sai
    {
        int n1=tuCanSua.length();
        int n=list.size();
        for(int i=0;i<n;i++) {
            String a = list.get(i).substring(0, n1);
            if (a.equals(tuCanSua)){
                list.set(i,tuCanSua+"\t"+"\t"+ yNghia);
                break;
            }
        }
    }
    public void nhap(String a,String b){
        list.
    } */

}
